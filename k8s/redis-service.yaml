apiVersion: v1
kind: Service
metadata:
  name: redis
spec:
  selector:
    app: redis
  type: ClusterIP         # Ensures internal-only access
  ports:
    - port: 6379          # Service port
      targetPort: 6379    # Pod port

